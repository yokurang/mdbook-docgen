[package]
name = "docgen"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
anyhow = "1"
walkdir = "2"
convert_case = "0.6"
chrono = { version = "0.4", default-features = false, features = ["clock"] }

